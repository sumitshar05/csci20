// library includes
// #include <library name>

/*
This line explains the comment "//", meaning to ignore everything, so it does not RUN which is important because it tells someone who is reading the code and someone who wrote the code what is happening exactly
the symbol "#" tells it to perform an instruction
the word include tells the language, which is c++ to bring in the library
the symbol <> basically hold the library name
the "library name" inside those <> is basically the placeholder for the name of the library
*/

#include<iostream>

/*
the symbol # basically tells C++ that this is an instruction
the word include means that we want to bring something into our program.
the symbol < > basically holds the name for the library. 
iostream is basically a library that gives us input and output, for example, like printing something on the screen.
*/

// specific objects from library
using std::cout;

/*
the word using tells us that we want to use something
std means the C++ library
the symbol:: means to look inside std
the word cout is basically used to print things on the screen
the symbol; which I think is one of the more important symbols because some people tend to forget to use it and it basically means that the intsruction has ended
*/

//entrypoint
int main(){
    cout<<"hello world";
    return 0
}

/* 
The word int means that the function will return an integer number
main is basically the name of the main function, and this is where the program starts running, I think
. The parentheses () were basically used for the information that can be given to the function.
The symbol that I used { means the beginning of the actual main function itself.
and everything inside the {} is part of the function


cout is basically used to print something in C++
The symbol << sends the words "hello world" in this case, and they can be displayed
We are basically trying to display "hello world" 
the symbol; means that this is which is the instruction is finished

return means that we are sending that value back from the main function 
The number 0 means that the program is finished
the: means that the instruction is finished. 
*/


